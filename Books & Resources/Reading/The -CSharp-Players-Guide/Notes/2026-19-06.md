🔁 Loops
while
Repeats while a condition is true.
Checks the condition before each iteration.
while (condition)
{
    // code
}
do...while
Executes the code at least once.
Checks the condition after the first iteration.
do
{
    // code
}
while (condition);
for
Best when the number of iterations is known.
for (int i = 0; i < 10; i++)
{
    // code
}

Structure:

Initialization
Condition
Update
foreach
Used to iterate through every element in a collection.
Simpler and more readable than for.
Cannot modify the collection through the loop variable.
Does not provide the index.
foreach (int number in numbers)
{
    Console.WriteLine(number);
}
break

Stops the loop immediately.

if (input == "exit")
    break;
continue

Skips the rest of the current iteration and starts the next one.

if (number == 12)
    continue;
📦 Arrays

An array stores multiple values of the same type.

int[] numbers = new int[5];

Initialize with values:

int[] numbers = { 1, 2, 3, 4, 5 };

Access an element:

numbers[0]

Modify an element:

numbers[2] = 10;

Get the array size:

numbers.Length

Loop through an array:

for (int i = 0; i < numbers.Length; i++)
{
    Console.WriteLine(numbers[i]);
}

Or:

foreach (int number in numbers)
{
    Console.WriteLine(number);
}
Copying Arrays
for (int i = 0; i < source.Length; i++)
{
    destination[i] = source[i];
}
Finding the Smallest Value
int smallest = int.MaxValue;

foreach (int number in numbers)
{
    if (number < smallest)
        smallest = number;
}
Calculating an Average
int total = 0;

foreach (int number in numbers)
{
    total += number;
}

float average = (float)total / numbers.Length;
Useful Syntax

Last element:

numbers[^1]

Range:

numbers[1..^1]